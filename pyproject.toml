[project]
name = "gpresult-backend"
version = "0.0.1"
license = { text = "GPLv2+" }
requires-python = ">=3.9"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
